[project]
name = "hackathon-nu26"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = [
    "shared",
    "services/processor",
    "services/api-gateway",
    "services/report-service",
]
